Складніше(з інтервью):

- Построить классы которые создают дерево каталогов и файлов, каждый каталог или файл имеет имя, дату создания. 
    Только каталог может в себе содержать другие каталоги
- Написать скрипт на OOP PHP который возвращает чаще всего встречающееся имя файла или каталога
Входные данные: 
- на метод который вычисляет чаще всего встречающееся имя файла или каталога подоется обьект каталога

 $iterator = new DirectoryIterator($file);
    foreach ($iterator as $fileinfo) 
    {
        echo $fileinfo->getFilename() . " " . $fileinfo->getType() . "<br>";
        
    }

    foreach ($this->dirs as $fileinfo) 
                    {
                        $this->get($fileinfo);
                    }